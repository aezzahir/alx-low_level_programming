#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a : first integer
 * @b : first integer
 * Return: 1 if c is a digit, 0 otherwise.
 */

int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
