#include "main.h"

/**
 * set_string - sets the value of a pointer to a char.
 * @s: the address of a pointer to a character
 * @to: the pointer to a character
 */
void set_string(char **s, char *to)
{
*s = to;
}
